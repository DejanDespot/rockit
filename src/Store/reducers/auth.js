import {updateObject} from "../../Utils";

const initialState = {
    loggedIn: false,
    loading: false
};

const reducer = ( state = initialState, action ) => {
    switch ( action.type ) {
        default:
            return state;
    }
};

export default reducer;