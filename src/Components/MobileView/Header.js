import React from 'react';
import styles from '../../Styles/MobileView/header.scss';

const header = () => (
    <div className={styles.header}>
        <div>Home</div>
    </div>
);

export default header;